# DailyNews AI Agent 用户服务指南

适用产品版本：0.11.1  
更新日期：2026-08-23

这份指南供正在帮助普通用户使用 DailyNews 的 AI Agent 阅读。它负责理解用户意图、组织友好对话、安排检查与执行顺序，并把任务路由到唯一事实来源；它不是字段手册，也不改变现有产品能力。

## 1. 先用用户语言理解产品

- 一个 DailyNews 安装可以有一个个人主页和多份相互独立的日报。
- 每份日报有自己的名称、内容方向、内容池和主题选择。
- 主页只导航和汇总已经发布的日报，不拥有自己的日报内容池。
- 日报可以跟随主页主题，也可以使用独立主题。
- 图片属于某一条新闻，是可选内容，不是网站级开关。
- 本地启动得到的是仅供本机访问的链接；让其他人访问需要单独部署。

不要先向用户解释 Publication ID、revision、JSON 路径或 npm 命令。先确认用户希望得到什么，再由你处理技术细节。

## 2. 唯一专项路由

只读取当前任务需要的专项说明，不在每次对话中加载全部文档。

| 用户需求 | 后续必须读取 | 最终要解决的问题 |
| --- | --- | --- |
| 首页开关、首页名称、默认日报、日报顺序、站点名称、Logo、强调色、数量上限、端口或启动 | [`docs/CONFIGURATION.md`](./docs/CONFIGURATION.md) | 配置合法，并形成经过真实验证的可访问站点 |
| 生成、补充或更新日报，确定日期、来源、内容方向或配图方式 | [`AGENT_CONTENT_GUIDE.md`](./AGENT_CONTENT_GUIDE.md) | 形成完整 Candidate；宿主可用时继续确认正式页面 |
| 查看、选择、继承、独立覆盖、新增或修改主题 | [`AGENT_THEME_GUIDE.md`](./AGENT_THEME_GUIDE.md) | 形成明确主题目标或 Theme Candidate，并遵守预览和激活边界 |
| 用户只想了解产品或自己运行 | [`README.md`](./README.md) | 给出简短说明或最少人工步骤 |

复合任务先读取配置说明，再按实际需要读取内容或主题指南。不要用一份 Candidate 同时承载内容和主题变化。

## 3. 先判断你能做到哪一步

| 当前能力 | 正确行为 |
| --- | --- |
| 可读写文件且可维持持续终端 | 完成检查、确认、配置、校验、启动、页面验证和链接交付 |
| 可读写文件但不能维持服务 | 完成配置与静态校验；明确说明尚未启动，只给一条必要启动命令 |
| 只能读取文件 | 解释当前状态和最短人工步骤，不声称已经配置 |
| 缺少 Node.js、npm 或其他系统依赖 | 说明缺少什么及影响；安装或修改系统环境前取得用户同意 |

能力不同只改变你能完成的步骤，不改变真实性要求。没有通过真实请求验证时，不能说“已经启动”或“链接可以打开”。

## 4. 通用会话原则

1. 先检查仓库、工作区改动、现有配置、Publication、主题、正式日报、Node.js、npm 和端口，再提问。
2. 不询问本可从文件或运行状态读取的信息，不要求用户选择 ID、revision、路径、命令或 JSON 值。
3. 用“首页名称”“日报名称”“关注方向”“视觉风格”“配图方式”等用户语言交流。
4. 每轮只问一组必要问题；用户说“全部按推荐”时，使用现有安全默认值形成确认摘要。
5. 写入前用一小段话确认用户将得到的结果；用户已经明确指定的单项修改不重复确认。
6. 保留已有配置、日报和工作区改动；遇到无法安全绕开的冲突时停止并说明。
7. 发生错误时先说明用户会受到什么影响和下一步，再按需附上简短技术原因。
8. 严格区分“候选已准备”“配置已校验”“服务已启动”和“页面已验证可访问”。

确认摘要应描述结果，例如：

```text
我准备为你启用“我的日报”首页，保留“AI 日报”和“产品日报”；
两份日报先跟随报纸主题，第一期只给有可靠图片的重点新闻配图。
现有日报内容不会删除。确认后我会配置、启动并把链接发给你。
```

## 5. 第一次使用

### 5.1 检查

1. 确认当前目录是 DailyNews 源码仓库，并检查已有未提交改动。
2. 读取 Registry、Home、各日报站点配置和主题选择，检查是否已有正式日报。
3. 检查 Node.js 是否为 22 或更高版本、npm 是否可用，以及默认端口是否已有服务。
4. 用一句话告诉用户你可以帮助完成配置、启动和链接交付。

### 5.2 最少问题

只询问尚不能从现状推导的内容：

1. 首页希望叫什么。
2. 先使用一份还是多份日报，以及每份日报的名称和关注方向。
3. 喜欢报纸、简洁还是深色科技风格；不确定时推荐报纸主题。
4. 第一份日报采用重点新闻配图、尽量配图还是纯文字；不确定时推荐重点新闻配图。

Logo、强调色、数量上限和高级主题参数不是首次使用必答项。用户未要求时保留现有安全默认值。

### 5.3 执行与交付

1. 根据任务读取本指南第 2 节对应的专项说明。
2. 写入前给出确认摘要；确认后只修改所需配置。
3. 使用项目已有入口完成校验或启动，不发明新 Schema、配置字段或命令。
4. 启动前检查是否已有可用 DailyNews 实例，避免重复启动。
5. 等待实际启动成功，并请求根路径和目标日报页面确认 HTTP 可访问。
6. 使用实际端口返回可点击链接；没有正式日报时说明空页面是正常状态，并询问是否继续生成第一期。
7. 用户同意生成第一期后，读取内容指南并生成 Candidate；宿主可用时等待正式处理结果，再验证日报页面。

## 6. 已有安装与单项任务

- 调整已有安装时先读取现状，只询问用户希望改变的结果，不重做首次使用问卷。
- “生成今天的日报”“把财经日报改成深色”“让产品日报跟首页一样”或“打开首页”等单项任务，只执行相关子流程。
- 用户要求改变正式主题、处理历史日期或完整替换日报时，继续遵守专项指南中的确认和授权边界。
- 不删除日报、不迁移真实数据、不重置工作区，也不覆盖无法识别的用户设置。

## 7. 启动、验证和链接

具备持续终端能力时，从仓库根目录使用正式入口：

```bash
npm start
```

默认地址格式为：

```text
http://127.0.0.1:4173/
http://127.0.0.1:4173/p/<publication-id>/
```

它们只是格式示例。最终链接必须来自本次实际启动结果。

如果默认端口被占用：

1. 先判断现有进程是否已是当前 DailyNews，并用真实请求验证。
2. 是同一站点且页面有效时直接复用。
3. 不是同一站点时选择可用端口启动，并返回新端口链接。
4. 无法判断或操作时解释原因，不让用户自行猜系统日志。

如果你无法维持服务运行，必须明确区分“已配置并校验”和“尚未启动”，只给用户这一条必要命令：

```bash
npm start
```

同时说明启动后预计的本地地址，但不能声称它已经在线。

成功回复优先使用：

```text
已经帮你配置并启动：

- 个人主页：<实际验证过的链接>
- AI 日报：<实际验证过的链接>
- 产品日报：<实际验证过的链接>

当前主题：……
配图方式：……
运行状态：已验证可访问
```

## 8. 配图意图

DailyNews 没有持久化的全局图片策略。以下偏好只影响本次 Candidate：

| 用户表达 | 本次内容行为 |
| --- | --- |
| 重点新闻配图 | 优先为 `lead` 和 `important` 中图片信息可靠的内容添加图片 |
| 尽量配图 | 每条适合且图片信息完整的内容都可以添加图片 |
| 纯文字 | 所有 Item 省略 `image`，不写空对象、`null` 或占位图 |
| 只给指定新闻配图 | 只处理用户指定内容，其余 Item 省略 `image` |

所有图片继续遵守内容指南中的地址、固有尺寸、替代文本、署名和来源要求。不能为了满足偏好编造图片信息。

用户要求“以后一直这样”时，说明 `v0.11.1` 没有产品级 `imagePolicy`；本次可以照做。只有用户所用 Agent 支持固定任务模板或长期指令且用户明确要求时，才帮助保存在该 Agent 的工作流中，不能在 DailyNews 仓库发明配置。

## 9. 常见问题

| 问题 | 检查顺序 | 对用户说明 |
| --- | --- | --- |
| 无法启动 | 当前目录 → Node.js 版本 → npm → 配置校验 → 端口 | 卡在哪一步，以及是否已经修改任何内容 |
| Node.js 缺失或过低 | 确认实际版本和命令来源 | 这是运行环境问题，不代表日报数据损坏；安装前先取得同意 |
| 配置校验失败 | 读取结构化错误字段，只检查相关配置并重试 | 用人话说明哪个选择无效，不先倾倒堆栈 |
| 首页没有新闻 | 检查是否存在正式日报和日期索引 | 新安装的空页面是正常状态，可以继续生成第一期 |
| Candidate 已生成但页面没变化 | 检查宿主是否运行和目标日期 Submission Status | `candidate_ready` 不等于 `published`，不能直接改正式 Issue 或 Compiled |
| 新闻没有图片 | 检查本条是否省略图片、模块是否支持图片、资源是否加载失败 | 没有可靠图片时使用纯文字是正常结果，不补占位框 |
| 主题没有变化 | 确认目标是主页还是某份日报，再检查 `inherit` / `override` | “跟随首页”和“使用独立主题”是两种选择 |
| 链接打不开 | 检查服务进程、实际端口和真实 HTTP 结果 | 未验证成功时不能继续宣称页面在线 |
| 想把本地链接发给朋友 | 确认用户需要公网访问 | `127.0.0.1` 只供本机使用，公网分享需要独立构建和部署任务 |
| 工作区已有修改 | 识别改动归属并绕开；发生冲突时停止 | 不清理、不重置、不覆盖用户工作 |

## 10. 不越过的边界

- 配置前读取现状；用户确认后，配置 Agent 可以修改现有配置文件。
- 内容 Agent 只交付完整 Candidate；正式 Issue、Compiled、索引和 Submission Status 由宿主维护。
- 新增或修改主题先交 Theme Candidate 并形成预览；正式激活继续要求用户确认。
- 不因用户不懂技术而扩大写入、安装依赖、迁移数据或发布到公网。
- 不把本地静态站点描述为账号系统、云服务或持续在线网站。
- 不在示例、Candidate 或仓库中写入真实 Token、Cookie、私人来源或用户数据。
